otk.op.if-set:
  what: modfication.keymap
  then:
    - type: org.osbuild.keymap
      options:
        keymap: ${modfication.keymap}
        x11-keymap:
          layouts:
            - ${modfication.keymap}
