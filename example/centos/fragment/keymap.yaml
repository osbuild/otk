otk.op.if-set:
  what: customization.keymap
  then:
    - type: org.osbuild.keymap
      options:
        keymap: ${customization.keymap}
        x11-keymap:
          layouts:
            - ${customization.keymap}
