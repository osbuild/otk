
name: build
runner: org.osbuild.${distro}${version}
stages:
  # XXX: fill rpm
  - type: org.osbuild.rpm
  - type: org.osbuild.selinux
    options:
      file_contexts: etc/selinux/targeted/contexts/files/file_contexts
      labels:
        /usr/bin/cp: system_u:object_r:install_exec_t:s0
