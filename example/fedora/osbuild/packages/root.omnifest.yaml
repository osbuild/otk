otk.merge:
  - include:
     - @core  # XXX
    exclude:
     - @core  # XXX
  - otk.inc:
      path:
        otk.var: 
          concat:
            - "version"
            - version
            - "/root.omnifest.yaml"
  - otk.inc:
      path:
        otk.var: 
          concat:
            - "architecture"
            - architecture
            - "/root.omnifest.yaml"
