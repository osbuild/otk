PREFIX = "otk."

PREFIX_TARGET = f"{PREFIX}target."
PREFIX_OP = f"{PREFIX}op."
PREFIX_CUSTOMIZATION = f"{PREFIX}customization."

NAME_VERSION = f"{PREFIX}version"
NAME_INCLUDE = f"{PREFIX}include"
NAME_DEFINE = f"{PREFIX}define"
