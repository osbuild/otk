otk.version: "1"

otk.define.name:
  otk.include: "02-include-in-define/example.yaml"
  subkey:
    otk.include: "02-include-in-define/example.yaml"

otk.target.osbuild.name:
  pipelines:
    - foo: ${foo}
      bar: ${bar}
      subkey:
        foo: ${foo}
        bar: ${bar}
