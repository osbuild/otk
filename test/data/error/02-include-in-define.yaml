otk.version: "1"

otk.define:
  otk.include: "02-include-in-define/example.yaml"
  subkey:
    otk.include: "02-include-in-define/example.yaml"

otk.target.osbuild:
  pipelines:
    - foo: ${foo}
      bar: ${bar}
      subkey:
        foo: ${foo}
        bar: ${bar}
